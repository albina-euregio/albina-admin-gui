// @ts-check
// Docker: env.js is regenerated at container start from env.template.js via
// envsubst (see Dockerfile), so the build-time value is just an empty placeholder.
/** @type {Partial<import("./environment-type").Environment>} */
globalThis.ENV = {};
